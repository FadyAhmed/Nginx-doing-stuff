http {
    include mime.types;

    limit_req_zone $binary_remote_addr zone=one:10m rate=1r/s;

    server {
        listen 8080;
        root "C:/nginx/sites";

        location /redirect {
            return 307 http://localhost:5000/feed/posts;
        }

        location /limited {
            limit_req zone=one;
            root "C:/nginx/sites";
        }
    }
}

events{}